// Namespace de At4FrameworkIntranet
Ext.namespace('At4FrameworkIntranet');

Ext.apply(At4FrameworkIntranet.FormDefaults, {
    /** Ancho por defecto de los campos numéricos
     * @type Number
     */
    NUMBER_FIELD_WIDTH: 59,

    /** Ancho por defecto de los campos de texto
     * @type Number
     */
    VERY_LARGE_TEXT_FIELD_WIDTH: 654,

    /** Ancho por defecto de los campos de texto
     * @type Number
     */
    LARGE_TEXT_FIELD_WIDTH: 487,

    /** Ancho por defecto de los campos de texto
     * @type Number
     */
    TEXT_FIELD_WIDTH: 320,

    YMEDIO_TEXT_FIELD_WIDTH: 490,

    /** Ancho por defecto de los campos de texto
     * @type Number
     */
    MEDIUM_TEXT_FIELD_WIDTH: 208,

    /** Ancho por defecto de los campos de texto
     * @type Number
     */
    SMALL_TEXT_FIELD_WIDTH: 152,

    /** Ancho por defecto de los campos de textarea
     * @type Number
     */
    SMALL_TEXTAREA_FIELD_WIDTH: 432,

    /** Ancho por defecto de los campos de textarea
     * @type Number
     */
    TEXTAREA_FIELD_WIDTH: 654,

    /** Alto por defecto de los campos de textarea
     * @type Number
     */
    TEXTAREA_FIELD_HEIGHT: 150,

    /** Ancho por defecto de los item selectors
     * @type Number
     */
    ITEMSELECTOR_FIELD_WIDTH: 318,

    /** Separador decimal
     * @type String
     */
    DECIMAL_SEPARATOR: ','

});
